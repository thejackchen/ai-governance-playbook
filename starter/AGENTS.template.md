# AGENTS.md

<!--
  驱动引擎模板 · AI 在你项目里的「工作规则全集」。
  宪法是「不变的真理」，这份是「真理怎么落地成日常动作」——每条规则都标注溯源到 L1-L4。
  用法：fork → 逐节按指引填 / 删用不上的 → 删掉所有 <!-- 注释 -->。
  原则：① 每条规则可溯源到宪法，否则删 ② 这文件也设行数硬上限（如 ≤80 行），超出先删再加
        ③ 操作细节下沉到 runbook，本文件只留「规则」别塞「步骤」。
-->

> 本文件是 [CONSTITUTION.md](CONSTITUTION.md) 的操作落地。每条规则可溯源到宪法 L1-L4，否则删。
> 修订需用户审。本文件 ≤ [待填：行数上限，如 80] 行。

## 0. 身份 [来自 L2]

<!-- AI 在这项目里扮演谁？默认角色是什么？若有多角色（协调器 / 工作线程）在此定义各自职责与边界。 -->
[待填：默认角色定义。例：主协调器——理解意图、评估复杂度、分类处理、知识同步；不写代码。]

## 1. 启动协议 [来自 L1+L4]

<!--
  新会话开场必读哪几类文件，按序列出。可迁移骨架 = 北极星 → 知识地图 → 当前坐标 → 工作记忆。
  把链接换成你项目真实路径。再写清「lint / 同步」的触发条件。
-->
依次读：
1. [CONSTITUTION.md](CONSTITUTION.md)（[待填：复读条件，如「最近 7 天未读时必读」]）
2. [待填：知识地图 / 索引文件路径] — 知识地图
3. [待填：当前坐标文件，如 PROJECT.md] — 在哪
4. [待填：路线 / 开放问题文件，如 ROADMAP.md] — 工作记忆

Lint / 知识同步触发条件（[待填：如 ≥20 commits 或 ≥7 天]）→ [待填：runbook 路径]

## 2. 对话中（即时响应）[来自 L2]

<!-- 用户抛出想法时，AI 当场怎么分类落库。核心：判断「这影响哪一层」，落到对应文件；冲突要暴露不要自作主张。 -->
用户表达想法 → 评估影响层 → 落到对应文件：
- 改变焦点 → [待填：当前坐标文件]
- 改变方向 / 优先级 → [待填：路线文件]
- 与已确认决策冲突 → **暴露冲突，等用户确认**
- 新领域认知 / 跨项目洞察 → [待填：知识库目录]（细分路由见 § 6）

## 3. 结束前（必跑，不可跳过）[来自 L3]

<!--
  每轮收尾的固定动作——这是「知识沉淀复利」落地的关键，最容易被跳过，所以要么机器化、要么写成铁律。
  典型 3 步：① 回顾几问（这轮有无新决策/焦点变化/旧事实失效/新洞察）② 追加一行 CHANGELOG ③ 跑 lint。
-->
1. [待填：收尾回顾几问 + 反事实检验，详细清单可下沉 runbook]
2. [待填：历史事件文件，如 CHANGELOG.md] 追加一行 `- [YYYY-MM-DD] [type] 摘要`
3. 跑 [待填：你的 lint / 校验命令，如 `npx tsx scripts/knowledge-lint.ts`]

## 4. 自主治理边界 [来自 L2]

<!--
  划清「AI 自己修 vs 必须等人拍板」。关键教训：协调器「有规则但不执行」比「没规则」更糟糕。
  自主修的应是结构性/机械性的（漂移、超行数、死链）；等人的是判断性的（业务、UX、决策升格、改宪法）。
-->
**自主修**（不问用户）：
- [待填：如 lint 触发命中 / 状态与实际偏差 / 治理文件超行数 / 死链]

**等用户拍板**：
- [待填：如 业务规则 / UX 形态 / 文案 / 功能取舍 / 决策升格]
- **CONSTITUTION.md 修订**（不得自动改）

## 5. 约束（不可违反）[来自 L1+L2]

<!-- 红线。违反即停。常见：与已确认决策冲突要停、外部理念以本地锚点为准非训练记忆、密钥不进 git、工作线程不得 merge 主干。 -->
- 与已确认决策冲突 → 停止，暴露冲突，不自动执行
- [待填：引用外部理念/方法论 → 以本地锚点页为准，缺失则暴露查证，不用训练记忆默认]
- 真实凭据 / 密钥不进 git
- [待填：其他项目红线]

## 6. 归档路由 [来自 L3]

<!--
  「这类内容该去哪个文件」的查找表——破解「知识散落 → 拼凑判断 → 拼错」恶性循环的关键。
  按你项目实际的目录结构填。每类内容只有一个权威去向。
-->
| 内容类型 | 去向 |
|---|---|
| 需求 / 任务 | [待填] |
| 已确认决策 | [待填：如 docs/decisions/（需用户确认）] |
| 领域 / 事实知识 | [待填] |
| 跨项目洞察 / 外部理念锚点 | [待填] |
| 设计草案 / 审计报告 | [待填：须含状态行] |
| 线程交接 / 时点快照 | [待填：只追加不回改] |
| 退役代码 / 资产 | [待填：须带 README 注明退役依据] |
| 历史事件 | [待填：如 CHANGELOG.md] |

## 7. Git 提交 [来自 L4]

<!-- commit 规范。可迁移骨架：`tag: 说明`，外加几个特殊 prefix（宪法/路线变更需特殊标记+人审）。 -->
格式 `tag: 说明`，tag 表 → [待填：commit 规范 runbook 路径]
特殊 prefix：`constitution: ...`（宪法修订，需用户审）/ [待填：其他特殊 prefix]
