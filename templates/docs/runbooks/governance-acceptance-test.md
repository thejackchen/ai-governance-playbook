# 治理验收测试套件 — 占位（正文见脚手架库根 acceptance-test.md）

> **本文件是占位指针，不是副本**——遵守 SoT「一处权威、不抄副本」（playbook 自己也守这条；验收套件正文只活一处，避免 playbook 内部两份漂移）。
>
> **完整模板**在本脚手架库根目录的 [`acceptance-test.md`](../../../acceptance-test.md)：6 环节（接手与理解 → 干活铁律 → 对话路由 → 知识沉淀+SoT → 结束前 → 自纠）× 占位用例 + 每环节一个填好的示例。
>
> **套用到你项目时**：把根 `acceptance-test.md` 复制到这个位置（你项目的 `docs/runbooks/governance-acceptance-test.md`），填掉占位、把机器判据里的路径 / grep 关键词改成你仓库真实的，然后**删掉本段指针、换成填好的正文**。治理大改后派无记忆 AI（优先真 CodeX / 清空记忆的 Claude）跑一遍，全过 = 治理健康。
