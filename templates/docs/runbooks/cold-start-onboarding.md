# 冷启动上手包 — 接手就能干活的装配清单

> [AGENTS 启动读序](../../AGENTS.md) 的下游。启动协议带你读完北极星 + 知识地图 + 工作台(地图 + 坐标);**本页补「地图 → 能干活」的最后一公里**。
> Last Reviewed: {YYYY-MM-DD:本页最近一次人工核对日期}

**本页是装配清单(BOM),不是知识本身**:只列「接手前要知道的 N 条 + 各自权威页」,正文几乎为零、内容靠链接 —— 复制正文必漂移(违反 SoT 一处权威)。**唯一例外**:协作个体偏好 + 部分本机工程事实只活在 AI memory / 不进 git,无记忆接手(CodeX / 新 clone)读不到,故在此留**可读浓缩**当兜底。

<!-- 填法提示:本页只在「指针 + 兜底浓缩」两种内容里二选一。任何能在仓库别处找到权威页的知识,这里只放链接、不抄正文。-->

**本页不重复**(各有专页,按本项目实际页名替换):{委派 / 交接机制 → 对应专页};{对话中影响层路由 → 对应专页};{目录改哪不改哪 → 对应专页}。

## 1. 协作契约(怎么和本项目用户协作)

> 完整偏好在 AI memory `feedback_*`(对有记忆的 AI 可见)。以下浓缩供 CodeX / 无记忆接手。性质 = **user-level 个体偏好,不是项目规则**,故**不进 AGENTS / CONSTITUTION 溯源链**(溯源链只收项目级、换人也成立的规则)。

<!-- 填法提示:列出与本项目用户协作时要注意的 N 条个体偏好(每条一句、可执行)。来源 = 你的 AI memory feedback_* + 与用户磨合出的脾气。下面是常见维度的占位骨架,按本项目真实情况增删改;留浓缩是为了让无记忆 / CodeX 也接得住。-->

- **{协作姿态:如「当合伙人不当助手——第一性原理挑战假设、独立探索多路径,别顺从」}**。
- **{设计决策归属:如「别让用户做设计决策——UI/UX/信息架构/文案自己查范式定方案、做出来再给看,AskUserQuestion 只留给真业务取舍」}**。
- **{用户输入的性质:如「用户建议是灵感不是指令,翻译成专业方案」}**。
- **{输入理解方式:如「语音输入按意图理解,同音/口误静默纠正,别抠字眼反问」}**。
- **{沟通风格:如「具体、简短;不中英混/不用代号;问决策落具体场景+给默认值」}**。
- **{排查归属:如「自己解决不让用户排查,bug 报告直接动手修」}**。
- **{工具/流程偏好:如「git 保持简单——纯文档/简单改动直接在 main,别开分支」}**。
- **{长任务节奏:如「前台快做这轮就给;后台跑定期汇报;说要派的 agent 必须真发出」}**。
- **{待办边界:如「用户独占事项(真机验收/外部操作)不写进 backlog」}**。
- **{UI 约束:如「不用 Emoji,用单色几何图标」}**。

> 行业级工作纪律(换谁都成立的、非个体偏好)沉淀在 insights,见 §2;以上仅个体偏好。

## 2. 干活前工程踩坑速查(碰到再查,别裸奔)

> 纯链接,指向 `KNOWLEDGE_BASE/insights/` 的权威页;这里只留一句「症状 → 哪页」,正文在专页。

<!-- 填法提示:每条 = 一句症状/触发条件 + 指向对应 insight 页的链接。内容随项目踩坑增长;新踩坑先沉淀成 insight 页,再在这里加一行指针。一条都没有就先留空、随工作积累。-->

**仓库已有页(权威,直接读)**:
- {一句症状/触发条件} → [{insight-页名}](../../KNOWLEDGE_BASE/insights/{insight-页名}.md)
- {一句症状/触发条件} → [{insight-页名}](../../KNOWLEDGE_BASE/insights/{insight-页名}.md)
- {…按本项目实际 insight 增删…}

## 3. 凭据 & 基础设施速查

> 凭据**文件位置**(非凭据本身)+ 关键基础设施的访问方式。机密本体在本机文件 / AI memory,**不进 git**。

<!-- 填法提示:这里只指「去哪找凭据 / 怎么连基础设施」,绝不写明文密钥。凡本机文件都标「不进 git、新 clone / CodeX 没有、需用户提供」。-->

- **凭据位置**:`{凭据文件路径,如 .claude/dev-credentials.md}`(用途)、`{部署凭据文件路径}`(用途)。**本机文件,不进 git** —— 新 clone / CodeX 没有,需用户提供。
- **{核心服务 SoT:如自托管后台/API 域名 + 部署方式}** → {指向对应 decision / 文档}。
- **{数据库 / 后端连接方式:首选途径 + 备选 + 兜底}**(详见 {对应 memory / 文档})。
- **{定时任务 / 调度基础设施:优先用什么、为什么}**(详见 {对应 memory / 文档})。
- **{网络 / 访问特殊事项:如某环境到某服务不通、绕行方式、换机是否需重配}**(详见 {对应 memory / 文档})。
