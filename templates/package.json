{
  "_comment": "骨架示例——别整体覆盖你项目已有的 package.json，只把下面 scripts.prepare、scripts.lint 和三个 devDependencies 合并进去。prepare 让 git hooksPath 在 npm install 时自动指向版本化的 .githooks/；否则 hook 不随 clone 生效 = 等于没装门禁（这是必须项，不是可选）。",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "lint": "tsx scripts/knowledge-lint.ts"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "^4.19.4",
    "typescript": "^5"
  }
}
