<!-- 模板:事件流水(append-only)。谁填:执行 AI 每轮收尾追加一行(landing 第 1 步);表头规则不改;填完删除本行注释与示例段。 -->
# CHANGELOG — {项目名} 事件流水

> 每轮收尾追加一行(只增不改)。最新在上。格式:日期段标题独占一行 `## YYYY-MM-DD`,其下条目以 `- ` 开头——心跳脚本按此格式统计回路活性,别改格式。
> 这是「做了什么」的流水账;「现在在哪」看 [ROADMAP 当前游标](ROADMAP.md),「为什么这么定」看 ADR 与判例。

## {YYYY-MM-DD}
- {做了什么 · 状态/留痕(commit 号 / 链接 / 可复现证据)}
