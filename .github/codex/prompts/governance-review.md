# PR治理审查

只读审查当前PR相对base的变化。不要修改文件，不执行git写操作。

检查：

1. 改动是否违反AGENTS/CLAUDE中的项目边界；
2. 是否引入新的顶层目录、跨层依赖或架构原语而没有ADR；
3. 完成宣称是否有测试或可复现证据；
4. 是否存在密钥、生产数据、权限放宽或危险操作；
5. 文档与现实是否产生新的冲突。

每个发现必须给出文件和行号；无法核实则明确写“无法核实”。输出按严重级别排序。没有发现时说明剩余测试空白。本审查是建议层，不负责合并决定。
