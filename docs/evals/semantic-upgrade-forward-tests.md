# 语义升级无上下文验收

## 验收合同（先定后测）

依据 [REQ-GOV-004](../requirements/specs/REQ-GOV-004-project-discovery-release.md) 当前负责人授权。模板传达语义，项目 AI 自主适配；不以模板字节一致、复制文件或版本号作为成功条件。

每个执行 Agent 使用无历史上下文，只给母版入口、合成项目位置和任务，不透露答案路径或评分项。执行者不改母版，不提交推送部署。主代理独立审查文件差异、复跑项目验证并评分；同一执行者自报分数不作验收。候选源只作隔离演练，不冒充线上已发布版本。

| 项目 | 分值 | 可证伪证据 |
|---|---:|---|
| 理解模板语义 | 20 | 区分建议与项目事实；没有要求负责人逐项点名、也不把所有差异叫冲突 |
| 自主实际适配 | 20 | 在可适配场景产生真实治理变更，保留合适的原布局或等价载体；只有报告不算落地 |
| 权威与修改边界 | 20 | 不碰业务/WIP/秘密/生产，不擅自裁决相互冲突的负责人真源 |
| 验证与版本真实性 | 20 | 实跑确定性验证，区分候选演练、独立语义验收和已发布采用；不只改版本号 |
| 可接续与缺口表达 | 20 | 索引可达、映射有理由，下一会话能找到状态；离线/歧义/客户端未验明确报告 |

通过门槛：每场景至少 90/100，且无事实覆盖、测试弱化、伪造采用或越权四类阻断错误。冲突场景的“自主实际适配”以正确隔离冲突并推进不冲突范围评定，不能为得分强行裁决。

## 当前结果

首轮及模板修订后的全新实作已由主代理独立审查。无上下文实作验收通过；发布状态见 ROADMAP。代码契约、Hook 包装器 fixture 与真实客户端 UI 信任分别记录；前两者不能冒充后者。

| 执行者/场景 | 理解 | 自主适配 | 边界 | 验证真实性 | 接续 | 总分/结论 |
|---|---:|---:|---:|---:|---:|---|
| `luna_cold_adapt_a` / 无冲突定制项目首轮 | 14 | 14 | 20 | 20 | 14 | 82，不通过，需修改模板后全新上下文重测 |
| `luna_cold_adapt_b` / 权威冲突项目 | 20 | 18 | 20 | 20 | 18 | 96，通过冲突处理场景，不是正式采用成功 |
| `luna_cold_adapt_a_retest` / 修订模板后全新无冲突项目 | 20 | 18 | 20 | 20 | 18 | 96，通过隔离语义适配，不是线上版本采用 |

### 首轮实际观察

- A：保留 `INDEX.md → knowledge/INDEX.md` 布局、原项目事实、未跟踪 WIP 与旧版候选锁边界；实际新增游标、适配记录与薄桥接，项目 `node scripts/verify-facts.mjs` 通过。但新游标又要求负责人确认项目意图和是否采用治理候选，在无真实冲突且已授权的隔离适配任务中重建了人工确认门。这正是本轮要避免的误读，不能只因代码检查通过就结账。
- B：保留同一部署主题下两份相反的 owner 记录，没有靠文件顺序擅定真源。实际新增游标、索引路由、问题队列与语义映射；问题只暂停争议自动部署，记录不冲突治理部分已落地。主代理复跑 `node scripts/verify-facts.mjs` 与 `git diff --check` 均通过；源业务和锁未变。最终母版 `governance-update.mjs --target . --offline` 返回 unknown 且未写目标；施工期间旧 export 短暂不匹配已作为历史失败降级，不留作当前阻断。
- 两场景只提供 README 入口、合成项目和边界，不提供评分答案。原始夹具分别位于 `tests/fixtures/semantic-adaptation/project-a`、`project-b`；试验基线为隔离 Git 提交 `bdb0221`、`19319c9`，WIP 不纳入基线。执行者未提交推送、未联网部署。以上证据不能外推为消费仓生产状态、真实 UI trust 或线上升级完成。

### 载体审查修复

主代理负测复现并要求修复：非法来源凭据片段出现在返回对象；仅合法版本号被标作已验证；离线仍输出新版适配步骤；坏配置被默认值掩盖。修复后独立断言确认返回对象与输出均不含合成秘密、无虚假 verified 字段、离线仅状态。版本探测与 Hook 已有专项测试，最终完整计数在发布前回填。

### 修订后的全新 A 实作

- 未修改失败试卷。以原始 A 夹具另建隔离目录 `/tmp/governance-semantic-a-retest.fppZpk`，基线 `671a8e0`；全新无历史 Luna Max 只获母版 README 入口和任务，未提供答案、评分项或首轮失败内容。
- 实际新增 `governance/ROADMAP.md`、语义映射与项目原生治理验证器；沿原有 `INDEX.md → knowledge/INDEX.md` 导航接入。保留 generic/lite、manual/notify 和合成来源，未安装不适用的运行时合同。
- 主代理读取完整 diff、新增文件与锁：原事实验证器和 release-posture 正本无变化；WIP SHA256 为 `88f3f1c9705f686aadd7a3ec2717daedb2b0ff01084856838c6b850685394a6a`，原验证器复跑通过。新验证器复跑通过，`git diff --check` 通过；没有弱化原测试。
- 游标只等待独立语义证据，不要求负责人重批意图或是否升级；4.1.0 仍为记录基线，4.3.0 为未发布演练候选。未自评、未伪称正式采用或真实 Hook 已生效。
- 扣分边界：映射与机器检查仍是小型合成项目、部分文本形状检查，不等于任意业务项目适配质量；候选试卷的额外记录不是通用强制 schema。两项实作得分为主代理独立评分，不是执行者自报。

## 主代理确定性复验

- `node scripts/governance-verify.mjs --ci`：162/162，0 failed、0 skipped；治理 lint 0 error/0 warn，kit 校验 0 error。
- `node --test tests/semantic-governance-update.test.mjs tests/discovery-docs-contract.test.mjs tests/semantic-adaptation-fixtures.test.mjs`：27/27，通过真实挂起子进程 SIGKILL、fetch/reader 截止、坏配置零探测、脱敏、离线和事实/WIP/只改版本号反例。
- 新文件进入 Git 暂存范围后：已声明源码 150/150，文档索引 23/23；工作线与仓外未声明全集仍为 unknown，不宣称全项目 100%。启动地图 31 行/3250 bytes，低于 60 行/6144 bytes。
- root/template CLI、库与共享 Hook 字节一致；`git diff --check` 通过。离线 CLI 不联网、不写；发布前真实在线 CLI 回读线上 4.2.0、本地 4.3.0，正确报告本地领先且不降级、不假称发布。
- 真实客户端 UI trust 和所有消费仓接线/采用未验；本轮证明仓库适配器及 Hook fixture，不证明所有客户端自动注入。旧客户端缺载体或配置 off/manual 时不能凭母版发布声称已经发现新版。
